interface:
  display_name: "Memory Maintenance"
  short_description: "Audit memory quality and graph health"
  brand_color: "#2563EB"
  default_prompt: "Use $memory-maintenance to audit semantic memory for stale, duplicate, or contradictory project facts."
dependencies:
  tools:
    - type: "mcp"
      value: "semantic_memory"
      description: "Local semantic-memory MCP server with recall, provenance, graph, and lifecycle tools"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
